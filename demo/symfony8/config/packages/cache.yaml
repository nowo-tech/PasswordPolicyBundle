framework:
    cache:
        # Default: filesystem (single pod / dev). See docs/examples/expiry-flash-and-cache.yaml
        #prefix_seed: your_vendor_name/app_name

        # --- Redis (recommended: FrankenPHP workers, Kubernetes multi-pod) ---
        #app: cache.adapter.redis
        #default_redis_provider: '%env(REDIS_URL)%'

        # --- Memcached (shared throttle + enable_cache) ---
        #app: cache.adapter.memcached
        #default_memcached_provider: '%env(MEMCACHED_URL)%'

        # --- Dedicated pool: Redis (isolate flash throttle from cache.app) ---
        #pools:
        #    password_policy.flash_throttle:
        #        adapter: cache.adapter.redis
        #        provider: '%env(REDIS_URL)%'

        # --- Dedicated pool: Memcached ---
        #pools:
        #    password_policy.flash_throttle:
        #        adapter: cache.adapter.memcached
        #        provider: '%env(MEMCACHED_URL)%'

        # --- Not for multi-pod (reference only) ---
        #app: cache.adapter.filesystem
        #app: cache.adapter.apcu
